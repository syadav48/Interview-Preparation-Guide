var numberOfSubarrays = function(nums, k) {
    let start = 0
    const isNice = (arr) => arr.every((e) => e % 2 !== 0)
    for(let i = 0; i < k; i++){
        console.log(i);
        
    }
};
console.log(numberOfSubarrays([1,1,2,1,1], 3));
console.log(3 % 2);
